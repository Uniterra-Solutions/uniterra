# Acceptance — dsh 0.1.5 migration: plugin set, toolchain, and family upgrade (milestone v0.17.0)

One row per requirement. `REQ-n` is GitHub issue **#(n+33)**. Every evidence entry names
something that exists or can be run in this repo today — a real test file, a real package
script, a real verification script — never an invented one.

Evidence standard (agreed with the user): a **local macOS run** covers the dev path, and a
**CI-triggered gate run** (`scripts/verify-cli-container/run.sh` in Docker,
`scripts/verify-windows-install/verify.ps1` on `windows-latest`) covers the container and
Windows paths. Each recorded piece of evidence is a command plus its output summary, kept
re-runnable; nothing is verified against the user's only copy of live data.

| Req          | Objective                                                                                                                                        | Verifiable evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| REQ-1 (#34)  | Turn completion raises exactly one native notification with session title + status; the menu checkbox persists the preference per profile        | New purpose-named tests in `packages/uniterra-desktop/test/` (turn-end → notification payload; an intermediate subagent turn produces none; checkbox state ↔ `.uniterra.json` round-trip) run by `pnpm --filter @uniterra-solutions/uniterra-desktop test`; plus one recorded macOS run showing a single OS notification and the toggle surviving an app restart                                                                                                                                        |
| REQ-2 (#35)  | `dsh-ego-browser` is provisioned as a vendored built-in from a pinned commit and one `ego_*` tool works in a session                             | `packages/uniterra-desktop/test/builtin-pbt.test.mjs` green via `pnpm --filter @uniterra-solutions/uniterra-desktop test` (vendor registry row → expected bundle); `vendor/dsh-plugins/VENDOR.md` pin-ledger row and `docs/modules/vendor-plugins.md` vendored-table row present; recorded fresh-profile boot calling one `ego_*` tool (snapshot or navigate)                                                                                                                                           |
| REQ-3 (#36)  | `dsh-browser-playwright` is retired and healed out of an already-provisioned profile, touching nothing else                                      | `packages/uniterra-desktop/test/builtin-pbt.test.mjs` green (retired row + `removeRetiredBuiltins` heal over a fixture profile carrying the row/dependency/copy) via `pnpm --filter @uniterra-solutions/uniterra-desktop test`; `pnpm-workspace.yaml` no longer lists `dsh-browser-playwright@0.1.1`; `docs/modules/vendor-plugins.md` + `docs/tech-stack.md` + `README.md` rows updated. Landing must be after REQ-2's recorded `ego_*` success                                                        |
| REQ-4 (#37)  | `dsh-file-upload` is retired and healed out; the native 0.1.5 upload path covers an attachment send                                              | `packages/uniterra-desktop/test/builtin-pbt.test.mjs` green (retired row + heal) via `pnpm --filter @uniterra-solutions/uniterra-desktop test`; `pnpm-workspace.yaml` entry removed; docs rows updated; recorded attachment send on a 0.1.5 profile. Landing must be after REQ-11                                                                                                                                                                                                                       |
| REQ-5 (#38)  | `dsh-subagent-model-picker` is retired and healed out; native `subagent` covers model selection                                                  | `packages/uniterra-desktop/test/builtin-pbt.test.mjs` green (retired row + heal) via `pnpm --filter @uniterra-solutions/uniterra-desktop test`; `pnpm-workspace.yaml` entry removed; docs rows updated; recorded native delegation naming an explicit `provider` + `model`                                                                                                                                                                                                                              |
| REQ-6 (#39)  | The pi-ai dependency and the `uniterra-systemprompt` package are gone with every reference cleaned up                                            | `grep -rn "uniterra-systemprompt\|pi-coding-agent"` (excluding `node_modules`, `dist`, `vendor`, `.git`, `CHANGELOG.md`, and the planning/run artifacts `.plan/` and `.dsh/`) returns nothing; `packages/uniterra-systemprompt/` absent; `tsconfig.json` references no longer list it; `.github/workflows/ci.yml` no longer runs its test step; `pnpm install --frozen-lockfile`, `pnpm run build`, `pnpm run typecheck`, `pnpm run lint` all green                                                     |
| REQ-7 (#40)  | `typescript-eslint` 8.70.x and `@types/node` 22.20.2, nothing else moved                                                                         | `pnpm-lock.yaml` shows `typescript-eslint@8.70.x` and `@types/node@22.20.2`; `git diff` touches only those two specs; `pnpm run lint` and `pnpm run typecheck` green                                                                                                                                                                                                                                                                                                                                    |
| REQ-8 (#41)  | ESLint 10 with unchanged rule intent and ignore behaviour                                                                                        | `pnpm-lock.yaml` shows `eslint@10.x` and `@eslint/js@10.x`; root `pnpm run lint` green plus each package's own `pnpm --filter <pkg> run lint` (`uniterra-desktop`, `uniterra-provider`, `uniterra-cli`); itemised record of every rule rename/removal the upgrade forced                                                                                                                                                                                                                                |
| REQ-9 (#42)  | `lint-staged` 17 with an equivalent pre-commit path                                                                                              | `pnpm-lock.yaml` shows `lint-staged@17.x`; a real staged-`.ts` commit attempt runs `.husky/pre-commit` to success at `--max-warnings 0`; root `package.json` `lint-staged` mapping + `.husky/pre-commit` unchanged or equivalently mapped; `pnpm-lock.yaml` gains no `yaml` dependency                                                                                                                                                                                                                  |
| REQ-10 (#43) | TypeScript 6.0.3 (not 7.x) with unchanged build behaviour                                                                                        | `pnpm-lock.yaml` shows `typescript@6.0.3`; `pnpm run build` (`tsc -b`), `pnpm run typecheck`, `pnpm run lint` green; no new `@ts-ignore`/`@ts-expect-error` (grep count unchanged); each tsconfig adjustment listed with its reason                                                                                                                                                                                                                                                                     |
| REQ-11 (#44) | All three dsh pins are consistent at `0.1.5-rc.2` and the vendored harness reports it; no adaptation code                                        | `pnpm install --frozen-lockfile` green; `pnpm-lock.yaml` resolves `@deepseek-ai/*` at exactly `0.1.5-rc.2`; `packages/uniterra-desktop/package.json` + `packages/uniterra-provider/package.json` peer/devDeps + the `patchedDependencies` keys agree; `pnpm run build:vendored-dsh` succeeds and `node vendor/dsh-harness/apps/cli/lib/bin.js --version` prints `0.1.5-rc.2`; `vendor/dsh-harness/VENDOR.md` ledger row + divergence status updated                                                     |
| REQ-12 (#45) | Patches match the 0.1.5 tarballs, every entry is paired, and alias-dependent built-ins still load                                                | `pnpm-workspace.yaml` `patchedDependencies` keys correspond one-to-one with `patches/` filenames (no dangling entry, no orphan file); `pnpm install` applies them without error; each retained patch header states its removal condition; a 0.1.5 profile loads the plugins that need the aliases (recorded boot)                                                                                                                                                                                       |
| REQ-13 (#46) | `preset-compat` is converged on 0.1.5 with the never-overwrite guarantee intact, and an upgraded profile creates + resumes sessions              | `packages/uniterra-desktop/test/preset-compat-pbt.test.mjs` and `preset-compat-regressions.test.mjs` green via `pnpm --filter @uniterra-solutions/uniterra-desktop test` (including the never-clobber guarantee); a scripted upgrade run on a copied pre-upgrade profile (settings `default: code` + v0/v1/v2 session logs) creating a new session and resuming an old one, recorded; module comment + docs state which layer still needs the shim and its removal condition                            |
| REQ-14 (#47) | The provider works on 0.1.5 — section mount-order, both protocols, image input — with a backward-compatible config format                        | `pnpm --filter @uniterra-solutions/uniterra-provider test` green (includes `test/provider-recognition.test.mjs` mount-order PBT and `test/image-modality.test.mjs`), plus `lint`/`typecheck`; a recorded real conversation with one tool call and one image input on a 0.1.5 profile                                                                                                                                                                                                                    |
| REQ-15 (#48) | Every npm built-in has a recorded 0.1.5 verdict (load + one key action); better-sidebar runs 0.19.0                                              | A per-built-in result table recorded in the issue (one row per npm built-in: `dshmarket`, `dsh-better-sidebar`, `dsh-find-plugin`, `dsh-tool-git`, `dsh-computer-use`, `dsh-git-worktree`, `ego-browser`), each with load + one key action; `dsh-better-sidebar@0.19.0` in `packages/uniterra-desktop/src/builtin.ts` and `pnpm-workspace.yaml`, with the sidebar integration exercise recorded; `pnpm --filter @uniterra-solutions/uniterra-desktop test` green                                        |
| REQ-16 (#49) | Vendored pins at workflow v0.1.4 / deep-whale v0.1.2 with the patch verdict recorded, and `run_workflow` working on 0.1.5                        | `vendor/dsh-plugins/VENDOR.md` pin-ledger rows updated for both plugins, including the re-plant-or-remove verdict; recorded `run_workflow` of one of the three pipeline capsules on a 0.1.5 profile; `packages/uniterra-desktop/test/workflow-*.test.mjs` green via `pnpm --filter @uniterra-solutions/uniterra-desktop test`; recorded deep-whale load in a profile whose `.uniterra.json` enables it                                                                                                  |
| REQ-17 (#50) | All 34 shortcuts verified on 0.1.5, failures repaired or explicitly disabled, suite + smoke green                                                | A 34-row coverage table recorded in the issue (working / repaired / disabled with reason — no untested row); `packages/uniterra-desktop/test/dsh-shortcuts-*.test.mjs` green via `pnpm --filter @uniterra-solutions/uniterra-desktop test`; `scripts/verify-dsh-shortcuts-smoke/run.sh` green; `vendor/dsh-plugins/VENDOR.md` patch notes updated with removal conditions                                                                                                                               |
| REQ-18 (#51) | Desktop runtime integration verified on 0.1.5 across readiness, CLI args, env contract, `$DSH_HOME` layout and cleanup; dev + packaged both boot | `packages/uniterra-desktop/test/dsh-cli-path.test.mjs` and the READY section of `builtin-pbt.test.mjs` green via `pnpm --filter @uniterra-solutions/uniterra-desktop test`; `packages/uniterra-desktop/test/workflow-capsule-provision-pbt.test.mjs` green; recorded dev boot and packaged boot each reaching an operable UI on 0.1.5, with bundled skills visible and `run_workflow` available; every surface change itemised                                                                          |
| REQ-19 (#52) | The full old-profile → 0.1.5 upgrade path and the release gates pass, with the risk documented                                                   | `scripts/verify-cli-container/run.sh` green; `scripts/verify-windows-install/verify.ps1` green on the CI release gate (or an explicit not-run reason + catch-up plan, and a blocking gate may not stay unrun); a recorded upgrade run on a **copy** of a pre-upgrade profile: new session created, old session resumed, every built-in loaded including `ego_*`, `run_workflow` exercised; `docs/` carries the Session V3 one-way-upgrade risk and rollback guidance; defects filed as follow-up issues |

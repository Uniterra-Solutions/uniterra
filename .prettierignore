node_modules/
dist/
# Build output: the provider emits its esbuild bundle to lib/ (eslint ignores **/lib/ for the same reason)
lib/
.dsh/
*.log
.tsbuildinfo
pnpm-lock.yaml
vendor/
# Vendored skill content from the Hermes plugins — keep byte-identical for clean re-syncs
packages/skills/src/skills/

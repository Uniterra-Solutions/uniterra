{
  "name": "@uniterra-solutions/uniterra-desktop",
  "version": "0.11.12",
  "private": true,
  "description": "Uniterra desktop shell: supervises a bundled DeepSeek Harness (dsh) CLI and hosts its Web UI in an Electron window. Runs dsh against the user's normal ~/.dsh (dev uses a mirrored test home); built-ins are ensured into the profile at startup.",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit",
    "lint": "eslint src",
    "dev": "electron .",
    "start": "electron .",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "prepare": "echo 'Runtime is built by the uniterra CLI locally (uniterra setup); nothing to prepare.'",
    "package": "pnpm run build && electron-builder --mac --publish never ${UNITERRA_BUILD_VERSION:+-c.extraMetadata.version=$UNITERRA_BUILD_VERSION}"
  },
  "dependencies": {
    "@uniterra-solutions/uniterra-updater": "workspace:*"
  },
  "devDependencies": {
    "@deepseek-ai/dsh": "0.1.0-rc.6",
    "@types/node": "^22.0.0",
    "electron": "^37.10.3",
    "electron-builder": "^25.1.8",
    "fast-check": "^4.9.0"
  }
}
